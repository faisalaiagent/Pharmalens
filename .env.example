# PostgreSQL connection string.
# IMPORTANT: append ?connection_limit=10&pool_timeout=30 to prevent
# P2024 "connection pool timeout" errors during `next build`.
DATABASE_URL="postgresql://user:password@host:5432/dbname?connection_limit=10&pool_timeout=30"

NEXT_PUBLIC_APP_URL="http://localhost:3000"
